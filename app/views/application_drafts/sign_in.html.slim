h1 Student Application for RGSoC #{current_season.name}

p
  | Please #{link_to 'sign in with GitHub', user_github_omniauth_authorize_path(redirect_to: request.original_url)}
    in order to apply for RGSoC #{current_season.name}.
