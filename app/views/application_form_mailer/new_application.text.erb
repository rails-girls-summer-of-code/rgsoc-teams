Created at: <%= @application.created_at %>

Students: <%= @application.team.students.map(&:name).join(', ') %>
Students' Emails: <%= @application.team.students.map(&:email).join(', ') %>

Application Data:
================
----------
<% @application.application_data.each do |field, value| %>
<%= field %>:

<%= value %>
----------
<% end %>
